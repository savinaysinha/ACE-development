

CREATE COMPUTE MODULE BLOB_TO_XML_Compute
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		DECLARE X BLOB;
		SET X = InputRoot.BLOB.BLOB ;
		CREATE LASTCHILD OF OutputRoot DOMAIN 'XMLNSC' PARSE(X);

		RETURN TRUE;
	END;
END MODULE;
	